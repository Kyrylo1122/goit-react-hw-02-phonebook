import styled from 'styled-components';
import {
  color,
  typography,
  space,
  layout,
  flexbox,
  border,
  shadow,
} from 'styled-system';
export const Box = styled.div`
  ${color}
  ${typography}
  ${space}
  ${layout}
  ${flexbox}
  ${border}
  ${shadow}
`;
